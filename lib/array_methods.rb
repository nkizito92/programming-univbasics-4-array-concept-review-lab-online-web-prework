def find_element_index(array, value_to_find)
  while value_to_find < array {
    array[value_to_find]
    value_to_find++
  }
end

def find_max_value(array)
  find_max_value(array)
end

def find_min_value(array)
  find_min_value(array)
end
